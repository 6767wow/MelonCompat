# MelonCompat Documentation

This wiki source now documents the C# package shape:

- `RoseMod.DevKit` is the reusable C# SDK.
- MelonLoader and BepInEx support comes from normal C# wrapper classes.
- The old RoseV language, sample mods, native bootstrap, and installer payload are no longer part of the repo.

Start with [Home](Home.md).
